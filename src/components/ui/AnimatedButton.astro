---
const { href = '#', label = 'Button' } = Astro.props;
---

<a
  href={href}
  class="group relative inline-flex items-center justify-center overflow-hidden rounded-2xl border border-white/15 bg-white/5 px-4 py-2 text-sm font-medium text-white/85 shadow-glass transition hover:border-white/25 hover:bg-white/10"
>
  <span
    class="absolute inset-0 -z-10 opacity-0 transition group-hover:opacity-100"
    style="
      background: radial-gradient(700px 240px at 20% 30%, rgba(124,58,237,0.35), transparent 55%),
                  radial-gradient(520px 240px at 80% 70%, rgba(34,211,238,0.25), transparent 55%);
    "
  ></span>
  <span class="mr-2 inline-block h-2 w-2 rounded-full bg-white/70" style="box-shadow: 0 0 16px rgba(34,211,238,0.7)"></span>
  <span class="translate-y-[0.5px]">{label}</span>
  <span class="ml-2 text-white/60 transition group-hover:translate-x-0.5 group-hover:text-white/85">→</span>
</a>
